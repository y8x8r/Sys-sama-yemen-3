# Worklog — سما اليمن للسفريات والسياحة

---
Task ID: 1
Agent: Main Agent (Super Z)
Task: بناء نظام إداري متكامل لمكتب سما اليمن للسفريات والسياحة بهوية بصرية بنفسجية مقاربة للصورة المرجعية.

Work Log:
- قراءة البرومبت الكامل (38KB) وتحليل المتطلبات: 21 خدمة منفصلة، 6 أقسام رئيسية، RTL، خط Cairo، ألوان بنفسجية.
- تحليل الصورة المرجعية عبر VLM — استخراج الباقة البنفسجية: #7C3AED → #A855F7، خلفية #F8F9FC، شريط جانبي أبيض.
- تهيئة مشروع Next.js 16 + TypeScript + Tailwind 4 + shadcn/ui عبر fullstack-dev skill.
- بناء Design System في globals.css: متغيرات CSS للهوية البنفسجية، حالات الشريط الجانبي (hover #F3E8FF، active تدرّج بنفسجي بنص أبيض)، pastel colors لبطاقات المؤشرات.
- إعداد خط Cairo في layout.tsx مع RTL ودعم اللغتين (ar/en).
- إنشاء types.ts (الأدوار، العملات، أنواع الخدمات، أنواع البيانات الكاملة).
- إنشاء translations.ts (قاموس ثنائي اللغة عربي/إنجليزي ~400 مفتاح).
- إنشاء mock-data.ts (10 عملاء، 5 موظفين، 3 مستخدمين، 16 خدمة موزعة على 8 أنواع، 12 فاتورة، 6 دفعات، 5 مصروفات، 8 سجلات تدقيق).
- إنشاء store.ts (Zustand + persist) لإدارة الحالة: المصادقة، اللغة، الثيم، التنقل، البيانات.
- بناء LoginPage مع لوحة جانبية بنفسجية متدرجة وأزرار الدخول التجريبي للأدوار الثلاثة.
- بناء Sidebar بـ 6 أقسام رئيسية: لوحة التحكم، الخدمات (21 خدمة فرعية)، الإدارات (3)، المالية (3)، المراقبة (3)، الإعدادات (2).
- بناء Topbar مع زر اللغة، الوضع الليلي/النهاري، الإشعارات، قائمة المستخدم.
- بناء DashboardPage بـ 5 بطاقات KPI باستيل، 7 widgets (حالة المعاملات، النظرة المالية، آخر العملاء، توزيع الخدمات، أفضل الخدمات، تقرير الأداء، تنبيهات التأشيرات).
- بناء ServicePage (مكوّن قابل لإعادة الاستخدام) + service-configs.ts يضم تكوينات الـ 21 خدمة بحقولها حرفياً كما ورد في البرومبت.
- بناء CustomersPage مع نموذج إضافة وبحث وترقيم تلقائي (CUST-00001).
- بناء EmployeesPage، AgentsCompaniesPage (تبويبان).
- بناء RevenuesExpensesPage (3 بطاقات ملخص + مخطط 14 يوم + جدولين)، PaymentsPage، InvoicesPage.
- بناء StatisticsPage (4 مخططات)، AuditLogPage، VisaExpiryPage (حساب 85 يوم من تاريخ الدخول).
- بناء UsersPermissionsPage (مصفوفة صلاحيات الأدوار الثلاثة + جدول المستخدمين)، SystemSettingsPage (لغة، ثيم، سياسات إلغاء، طرق دفع، أداء).
- ربط جميع الصفحات في page.tsx عبر PageRouter.

Stage Summary:
- تم تطوير النظام بالكامل ويعمل بنجاح على المنفذ 3000.
- تم التحقق من الواجهة عبر agent-browser: تسجيل الدخول يعمل، التنقل بين 21 خدمة + 11 صفحة أخرى يعمل، تبديل اللغة ar→en يعمل، الوضع الليلي يعمل، إنشاء العميل الجديد يعمل (CUST-00011).
- VLM أكد جودة 9/10: هوية بنفسجية متسقة، RTL صحيح، جميع الأقسام مرئية.
- ESLint نظيف بدون أخطاء.
- متوسط زمن الاستجابة: ~100ms للصفحات.
- بيانات تجريبية واقعية لـ 10 عملاء و16 معاملة و12 فاتورة و6 دفعات و5 مصروفات.
- الملفات الناتجة في /home/z/my-project/download/: dashboard-final.png, dashboard-english.png, dashboard-preview.png, invoices-dark.png, statistics.png, visa-expiry-data.png, audit-log.png

---
Task ID: 2
Agent: Main Agent (Super Z)
Task: تنفيذ التعديلات الرسمية المحددة النطاق (10 أقسام) على نظام سما اليمن — مع الحفاظ على كل ما هو خارج النطاق.

Work Log:
- تحديث types.ts: إضافة Notification، PermissionLevel، UserPermission، تحديث User ليحتوي passwordHash بدلاً من كلمة مرور مكشوفة.
- تحديث mock-data.ts: حذف جميع البيانات التجريبية (عملاء، خدمات، فواتير، مدفوعات، مصروفات، سجلات تدقيق، إشعارات). إنشاء حساب المدير العام الوحيد user1/sama1 على مستوى التهيئة. إضافة allServiceTypes للاستخدام في شاشة «تحديد نوع الخدمة».
- تحديث store.ts: إضافة إجراءات . حساب visaExpiry ديناميكياً عند كل تغيير في services.
- تحديث translations.ts: إضافة ~60 مفتاح جديد (role_general_manager، create_employee، f_employee_name، f_username، f_employee_password، perm_read/write/update/book/full، change_password، edit_customer/delete_customer، export_weekly/monthly، action_preview/delete/edit/print، select_service_type، add_expense، f_expense_purpose/date، footer_copyright، empty_* states، select_customer، no_customers_yet، auto_filled_passport).
- تحديث globals.css: إضافة فئات dark-mode متناسقة بالكامل — خلفية #0F0F1A، cards #1A1A2E، sidebar #16162A، pastel colors بنسخ داكنة، overrides لـ dialog/popover/dropdown/table-header. إضافة .app-footer.
- تحديث login-page.tsx: حذف قسم «بيانات تجريبية للدخول» نهائياً وأزرار الدخول السريع للأدوار.
- تحديث sidebar.tsx: التذييل يعرض «جميع الحقوق محفوظة لدى Sky Link 2026».
- تحديث dashboard-layout.tsx: إضافة تذييل سفلي بنفس النص.
- تحديث topbar.tsx: العلامة الحمراء تظهر فقط عند وجود إشعارات غير مقروءة، عداد رقمي بدلاً من نقطة. زر «تعليم الكل كمقروء». النقر على إشعار يضعه في حالة «تمت المشاهدة».
- تحديث dashboard-page.tsx: زر «إضافة معاملة» يفتح شاشة «تحديد نوع الخدمة» (21 خيار). حذف بطاقة صافي الربح نهائياً (4 بطاقات KPI فقط). حذف تقرير صافي الربح من تقرير الأداء. حالة فارغة رسمية عند عدم وجود بيانات.
- تحديث service-page.tsx: ترتيب الإجراءات (معاينة ← حذف ← تعديل ← طباعة). زر تصدير Excel بقائمة منسدلة (تصدير أسبوعي/شهري). اختيار العميل من سجل موحد (combobox). اعتماد رقم الجواز تلقائياً من ملف العميل مع رسالة «(معبأ تلقائياً من ملف العميل)». تعديل وحذف المعاملات مع تأكيد.
- تحديث service-configs.ts: إزالة fromCustomer من حقول (customerNumber, phoneNumber, passportNumber, nationalId) — أصبحت حقول نصية تُعبأ تلقائياً. fromCustomer يقتصر على customerName فقط.
- تحديث customers-page.tsx: إضافة تعديل وحذف مع تأكيد. تصدير Excel أسبوعي/شهري (CSV مع BOM لدعم العربية). حالة فارغة رسمية.
- تحديث employees-page.tsx: نموذج إنشاء حساب موظف بالحقول الأربعة فقط (اسم الموظف ← اسم المستخدم ← الدور ← كلمة مرور الموظف). اعتماد فوري بعد الحفظ. تعديل وحذف مع تأكيد. قسم تعديل الصلاحيات الدقيقة (قراءة/كتابة/تعديل/حجز/صلاحيات كاملة) لكل وحدة. قفل الصفحة لغير المدير العام.
- تحديث agents-companies-page.tsx: إضافة، تعديل، حذف للوكلاء والشركات مع اعتماد فوري وتوست تأكيد.
- تحديث invoices-page.tsx: طباعة فاتورة رسمية مهيأة للعربية وRTL. تعديل (المبلغ الإجمالي، المدفوع، الحالة) وحذف مع تأكيد. ترتيب الإجراءات (معاينة ← حذف ← تعديل ← طباعة). إزالة بطاقة صافي الربح (3 بطاقات فقط: الإجمالي، المدفوع، المتبقي).
- تحديث revenues-expenses-page.tsx: نموذج «إضافة مصروف» بالحقول المطلوبة (رقم المصروف تلقائي ← غرض الصرف ← تاريخ الصرف ← المبلغ + العملة). إزالة بطاقة صافي الربح (بطاقتان فقط: الإيرادات، المصروفات). إزالة صافي الربح من المخطط والجداول.
- تحديث system-settings-page.tsx: حذف قسم «منطقة الخطر» نهائياً. إضافة بطاقة «تغيير كلمة المرور» للمدير العام (كلمة المرور الحالية + الجديدة + التأكيد).
- تحديث users-permissions-page.tsx: تحديث أدوار الأذونات الثلاثة المعتمدة (مدير عام، مسؤول حجوزات، محاسب) بصلاحياتها المحددة. قفل الصفحة لغير المدير العام.

Stage Summary:
- تم تطبيق جميع بنود التعديل العشرة دون المساس بأي عنصر خارج النطاق.
- الحساب الأولي الوحيد: user1/sama1 بدور مدير عام. لا توجد حسابات تجريبية أخرى. لا تظهر بيانات الدخول في شاشة الدخول.
- تم حذف جميع البيانات التجريبية نهائياً. الواجهات تعرض حالات فارغة رسمية.
- تم حذف «منطقة الخطر» نهائياً من الإعدادات.
- الوضع الليلي متناسق بالكامل (خلفية، شريط جانبي، رأس علوي، تذييل، بطاقات، جداول، نوافذ منبثقة، قوائم منسدلة، حالات فارغة، مخططات).
- زر «إضافة معاملة» يفتح شاشة «تحديد نوع الخدمة» بدلاً من نموذج مباشر.
- ترتيب الإجراءات في جميع الخدمات: معاينة ← حذف ← تعديل ← طباعة.
- تصدير Excel أسبوعي وشهري في قائمة العملاء وفي قائمة كل خدمة.
- اختيار العميل من سجل موحد + اعتماد رقم الجواز تلقائياً من ملف العميل عند توفره.
- نموذج إضافة مصروف: رقم تلقائي ← غرض الصرف ← تاريخ الصرف ← المبلغ. لا يوجد خيار صافي الربح في أي واجهة.
- الإشعارات: العلامة الحمراء تظهر فقط للإشعارات غير المقروءة، وتُزال عند الاطلاع، وتُزال من الأيقونة عند عدم بقاء إشعارات غير مقروءة.
- التذييل: «جميع الحقوق محفوظة لدى Sky Link 2026» في كل صفحة.
- ESLint نظيف بدون أخطاء.
- VLM أكد: تقييم 9/10 للوضع الليلي متناسق بالكامل مع الهوية البنفسجية والتذييل ظاهر.
- ملفات لقطات الشاشة في /home/z/my-project/download/: dashboard-empty-light.png, dashboard-empty-dark.png, dashboard-final-light.png, dashboard-final-dark.png, notifications-test.png

---
Task ID: 3
Agent: Main Agent (Super Z)
Task: تنفيذ الإصلاحات الجذرية — استعادة كلمة المرور، تصحيح المؤشرات المالية، الطباعة النظيفة، تزامن الدفعات الذري، حفظ البيانات في Prisma+SQLite، إلغاء المعاملات بدلاً من حذفها، السياسات، التصدير المنظم.

Work Log:
- إعداد Prisma schema كامل (User, Employee, Customer, Agent, TransportCompany, ServiceRecord, Invoice, Payment, Expense, AuditLog, Notification, Policy) مع علاقات صحيحة وقيود.
- تهيئة SQLite (file:/home/z/my-project/db/custom.db) — حفظ دائم على القرص وليس تخزين متصفح.
- إنشاء seed script يُنشئ حساب user1/sama1 فقط مع أسئلة استعادة مجزّأة (SHA-256).
- بناء 15+ API route: auth (login/logout/forgot-password/change-password), customers, services, invoices, payments, expenses, employees, agents, companies, policies, notifications, stats, audit, export.
- استخدام db.$transaction لضمان العمليات الذرية: إنشاء معاملة + فاتورة + دفعة في معاملة واحدة. إذا فشل أي جزء تُلغى بالكامل.
- تزامن الدفعات: عند حفظ معاملة بمبلغ مسلَّم، يُنشأ سجل دفعة تلقائياً ويربطه بالعميل والخدمة والفاتورة.
- إعادة بناء store.ts بالكامل لاستخدام API calls بدلاً من Zustand persist. الجلسة عبر httpOnly cookie.
- صفحة الدخول: تدفق استعادة كلمة المرور للمدير العام فقط بسؤالين أمنيين (2024 + ahmed778495152@gmail.com). لا تظهر الإجابات في الواجهة أو السجلات.
- لوحة التحكم: مؤشرات مالية صحيحة مفصولة حسب العملة (SAR/YER/USD). اختيار العملة في المخطط. لا تجمع عملات مختلفة.
- صفحة طباعة الفاتورة: نسخة نظيفة بدون أزرار ×/إلغاء/طباعة. اسم المكتب "سما اليمن للسفريات والسياحة" بخط 32px.
- إلغاء المعاملات: DELETE يغيّر الحالة إلى "cancelled" مع سبب إلزامي. لا يحذف نهائياً. يُعكس الدفعات والفواتير المرتبطة.
- تصدير Excel (CSV مع BOM) و PDF (HTML قابل للطباعة) لكل من: العملاء، الخدمات، الفواتير، المدفوعات، المصروفات. مع خيارات يومي/أسبوعي/شهري.
- إزالة تكرار ملاحظات المكتب من 8 خدمات (تأشيرات عمل/زيارة، الشحن، التخليص، الموافقات الأمنية، التقارير الطبية، التأمينات، الحجوزات الفندقية).
- التعبئة التلقائية لرقم الجواز ورقم البطاقة من ملف العميل عند اختياره في نماذج الخدمات.
- تحذير مغادرة النموذج بتغييرات غير محفوظة (window.confirm).
- قسم السياسات في الإعدادات — مدير عام فقط يضيف/يعدل/يحذف.
- ربط globalThis للجلسات لضمان بقاءها عبر hot reloads.

Stage Summary:
- جميع البيانات محفوظة في SQLite على القرص — تبقى بعد تحديث الصفحة، تسجيل الخروج وإعادة الدخول، إغلاق المتصفح.
- الدخول بـ user1/sama1 يعمل. تغيير كلمة المرور عبر استعادة كلمة المرور يعمل (تم اختباره: newpass123).
- إنشاء عميل يعمل (CUST-00001). إنشاء معاملة يعمل (SRV-2026-00001). إنشاء فاتورة تلقائية (INV-2026-00001). إنشاء دفعة تلقائية (PAY-2026-00001).
- التزامن الذري مثبت: عند حفظ معاملة بمبلغ 3500 ر.س، ظهرت الدفعة PAY-2026-00001 بقيمة 3500 ر.س مرتبطة بالفاتورة INV-2026-00001.
- المؤشرات تتحدث فوراً: بعد حفظ المعاملة، لوحة التحكم أظهرت "الإيرادات المحصلة اليوم: 3,500 ر.س".
- إلغاء المعاملة يعمل: تم تغيير الحالة إلى "ملغاة" مع حفظ السبب "إلغاء بناءً على طلب العميل". المؤشرات تحدثت (الإيرادات رجعت إلى 0، المعاملات النشطة 0، ملغاة 1).
- البيانات تبقى بعد إعادة التحميل: العميل والمعاملة لا يزالان موجودين.
- ESLint نظيف.
- الملفات الناتجة: dashboard-final-v3.png

---
Task ID: 5
Agent: Main Agent (Super Z)
Task: إصلاح شامل — فواصل القائمة الجانبية، طباعة الفواتير، تصدير PDF، الخصم المحاسبي للمصروفات

Work Log:
- إضافة فواصل بصرية واضحة في الشريط الجانبي:
  * فاصل `border-t` بين كل قسم رئيسي والآخر
  * فاصل دقيق بين كل عنصر فرعي داخل القسم الواحد
  * زيادة المسافات الرأسية (space-y-3 للأقسام، space-y-0.5 للعناصر الفرعية)
- بناء طبقة محاسبية مركزية (Accounting Service Layer) في src/lib/accounting.ts:
  * نموذج CashBox (الصندوق/الحساب) مع رصيد لكل عملة
  * نموذج TransactionLedger (سجل العمليات المالية) بنظام debit/credit
  * دالة debitCashBox — خصم مبلغ من صندوق
  * دالة creditCashBox — إضافة مبلغ إلى صندوق
  * دالة reverseTransaction — عكس حركة سابقة
  * دالة getDefaultCashBox — الحصول على الصندوق الافتراضي للعملة
  * كل العمليات ذرية (db.$transaction)
- ربط إضافة المصروف بالخصم المحاسبي:
  * عند إنشاء مصروف، يُخصم المبلغ تلقائياً من الصندوق الافتراضي للعملة
  * يُسجَّل سجل debit في TransactionLedger
  * يُحدَّث رصيد الصندوق فوراً
  * يُربط المصروف برقم الحركة المالية
- إضافة API لإلغاء المصروف (/api/expenses/[id]/cancel):
  * يعكس الحركة المالية الأصلية (credit)
  * يُرجع المبلغ إلى الصندوق
  * يحدّث الرصيد
- إصلاح طباعة الفواتير:
  * إنشاء API جديد /api/print/invoice يُرجع HTML نظيف قابل للطباعة
  * زر الطباعة يفتح نافذة جديدة بدلاً من window.print()
  * صفحة الطباعة نظيفة بدون أزرار ×/إلغاء/طباعة
  * اسم المكتب "سما اليمن للسفريات والسياحة" بخط 32px
  * تعرض بيانات العميل والخدمة والمبالغ والتفاصيل
- إصلاح تصدير PDF:
  * كان خطأ ByteString بسبب أحرف عربية في Content-Disposition
  * ترميز اسم الملف بـ encodeURIComponent
  * استخدام filename*=UTF-8'' للدعم الكامل
  * زيادة خط اسم المكتب إلى 32px في تقرير PDF
  * إضافة خيار "تقرير يومي" للتصدير
- إضافة API للصناديق (/api/cashboxes) والعمليات المالية (/api/transactions)
- تنظيف invoices-page.tsx: إزالة printMode و useEخطاب القديم

Stage Summary:
- فواصل واضحة بين جميع الأقسام والعناصر في القائمة الجانبية
- طباعة الفواتير تعمل: تفتح نافذة جديدة بفاتورة رسمية نظيفة
- تصدير PDF يعمل: تقرير منظم بـ RTL مع اسم المكتب والتذييل
- النظام المحاسبي يعمل: عند إضافة مصروف 500 ر.س، تم خصمها من CASH_SAR (رصيد -500)
- سجل العمليات المالية يسجل كل حركة مع رقم TX ورقم الصندوق والمستخدم
- تم اختبار: TX-2026-00001 debit 500 SAR من CASH_SAR لمصروف EXP-2026-00002
- ESLint نظيف
